---
"@decentralized-design/ghost": minor
---

Rename the public package from `@anarchitecture/ghost-fingerprint` to `@decentralized-design/ghost`. The `ghost` and `ghost-fingerprint` bins, all subpath exports (`/core`, `/fingerprint`, `/scan`, `/cli`), and the `.ghost/` package format are unchanged; only the npm name moves. Release tags and tarballs now use the `decentralized-design-ghost` prefix.
