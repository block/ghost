---
"@anarchitecture/ghost": minor
---

Collapse the fingerprint graph to a flat catalog: remove the `checks`, `review`, and `migrate` commands, drop node `relates` edges and slice traversal, and reduce `gather` to emitting the menu the agent selects from.
