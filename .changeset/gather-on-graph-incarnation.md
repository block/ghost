---
"@anarchitecture/ghost": minor
---

`ghost gather` now composes its context slice by traversing the fingerprint node graph and emits nodes-by-provenance prose (own / ancestor / edge), and gains `--as <incarnation>` to filter the slice to one output form (e.g. email, billboard, voice) while always keeping essence (untagged) nodes.
