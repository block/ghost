---
"@anarchitecture/ghost-fingerprint": patch
---

Rewrite the package README onto the flat-corpus model: plain-language framing, current CLI commands only, and removal of stale graph-era shapes (`relates`, surface directories, `checks/` at the fingerprint root, `ghost checks`/`ghost review`/`ghost migrate`).
