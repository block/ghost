---
"@anarchitecture/ghost-fingerprint": minor
---

Add an optional `plugins` manifest key, serve haunt inventory as a materials section in `ghost gather`, and drop the index.md→core id mapping (the entrypoint node's id is now `index`).
